"""
main.py — composition root. Reads AppConfig, builds the isotp stack,
EcuUdsClient, CloudProvisioningClient, reporters, ProvisioningOrchestrator,
and runs it against a VIN from the Dynomerk UI (integration TBD).

STATUS: not yet implemented — build order step 7 (last).
"""
